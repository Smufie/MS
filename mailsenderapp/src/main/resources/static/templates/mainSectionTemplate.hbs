{{#each main-section}}
<article id="{{articleId}}" class="article-in-main-section">
    <h2>{{article-header}}</h2>
    {{#if isPersons}}
    <div id='table-space'>

    </div>
    {{/if}}
</article>
{{/each}}